<script type="text/javascript">
      window.splashImage = "<%= image_url("splash.png") %>"
      window.logo = "<%= image_url("facebook-f-logo.jpg") %>"
      window.defaultUserIcon = "<%= image_url("default_user_icon.png") %>"
      window.friendsIcon = "<%= image_url("friends.png") %>"
      window.companyIcon = "<%= image_url("company.png") %>"
      window.educationIcon = "<%= image_url("education.png") %>"

</script>

<% if logged_in? %>

      <script type="text/javascript">
            window.currentUser = <%= render(
                  "api/users/user.json.jbuilder",
                  user: current_user
            ).html_safe %>
      </script>
<% end %>


<main id="root">

</main>